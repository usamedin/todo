import { Context } from "joi";
import { HandlerEvent } from "../../types/Handler";

export async function sync(event: HandlerEvent, context: Context) {
  // const endpoint = 'https://graph.microsoft.com/v1.0/me/todo/lists';
  // const headers = {
  //   Authorization: `Bearer ${event.body.accessToken}`,
  // };
  // console.log('headers', headers)
  // try {
  //   return tasks
  // } catch (error: any) {
  //   console.log('ERROR2222', error.response.data, error.response.status, error.response.statusText)
  // }
}